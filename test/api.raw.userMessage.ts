describe('api.raw.userMessages', function () {
  this.slow(2000)
  this.timeout(5000)

  describe('.userMessagesList (respondent)', function () {
    it('should do untested things (for now)')
  })

  describe('.userMessagesIndex ()', function () {
    it('should do untested things (for now)')
  })

  describe('.userMessagesUnreadCount ()', function () {
    it('should do untested things (for now)')
  })

  describe('.userMessagesSend (respondent, text)', function () {
    it('should do untested things (for now)')
  })

  describe('.userMessagesMarkRead (id)', function () {
    it('should do untested things (for now)')
  })
})
