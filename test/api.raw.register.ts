describe('api.raw.register', function () {
  this.slow(2000)
  this.timeout(5000)

  describe('.registerCheckEmail (email)', function () {
    it('should do untested things (for now)')
  })

  describe('.registerCheckUsername (username)', function () {
    it('should do untested things (for now)')
  })

  describe('.registerSetUsername (username)', function () {
    it('should do untested things (for now)')
  })

  describe('.registerSubmit (username, email, password, modules)', function () {
    it('should do untested things (for now)')
  })
})
